## Agent skills

### Issue tracker

Issues are Beans stored under `.beans/` and managed with the `beans` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Beans use the standard triage tags. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository. See `docs/agents/domain.md`.
